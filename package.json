{
  "name": "examplecrud-sls",
  "version": "1.0.1",
  "description": "Crud example with serverless framework",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "AWS",
    "SLS",
    "NODEJS"
  ],
  "author": "Jonathan castillo",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.113.0",
    "@aws-sdk/lib-dynamodb": "^3.113.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "serverless-iam-roles-per-function": "^3.2.0"
  }
}
